import { ChartConfig } from "@/common/components/ui/charts";

export const chartConfig = {
    income: {
        label: "Ingresos",
        color: "#10b981",
    },
    expense: {
        label: "Egresos",
        color: "#ef4444",
    },
} satisfies ChartConfig;